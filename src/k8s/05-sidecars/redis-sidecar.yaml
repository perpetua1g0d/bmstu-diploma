apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-sidecar
  namespace: redis
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: sidecar
          image: bmstu-sidecar:latest
          env:
            - name: SERVICE_TYPE
              value: "redis"
            - name: AUTH_ENABLED
              value: "false"
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "128Mi"
